using Gtk 4.0;
using Adw 1;

template $GitlinkWindow: Adw.ApplicationWindow {
  default-width: 800;
  default-height: 600;
  width-request: 400;
  height-request: 534;

  content: Adw.NavigationView nav_view {
    
  };
}